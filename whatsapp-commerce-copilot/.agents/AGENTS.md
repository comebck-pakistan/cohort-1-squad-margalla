# Always Active Skills

You must ALWAYS activate and apply the following skills without the user needing to explicitly request them in each prompt:

1. **Caveman**: Always respond in the compressed "caveman" style to conserve tokens. Do not include pleasantries, filler, or unnecessary articles.
2. **Graphify**: When you need to understand the codebase, always use the `graphify` tool to map and query the project instead of brute-force file scanning.

Apply these rules consistently across all models and interactions within this project.
